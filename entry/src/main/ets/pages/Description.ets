@Entry
@Component
struct Description {
  @State message: string = "该元服务项目名称：你好哈尼"; // 鸿蒙 （超强谐音：哈尼）

  build() {
    Text("该页面不做任何设计，旨在说明，并提供需求！！！");
    /**
     * 借助Ai的全部提示词，均放在这里，供参考，也供Ai打造项目
     * 注：项目初始化github已完成https://github.com/wwwyyyxl/Hello_HaNi
     * 需求1.(已完成需求。不满意)
    # Role (角色设定)
    你现在是一位拥有多年企业级应用架构经验的 HarmonyOS 高级技术专家和布道师。你需要协助我编写出极其规范、优雅且具有极客感的 ArkTS 代码。

    # Context (背景上下文)
    我正在开发一款 HarmonyOS 元服务（Atomic Service），作为我求职展示的高级“个人名片”。该元服务需要在面试时通过扫码实现秒开，给面试官带来极致的原生交互体验。
    目前我正在编写主入口（Entry 模块）的首屏页面（Index.ets）。

    # Task (核心任务)
    请使用声明式 UI 框架（ArkUI）和 ArkTS 语言，为我生成“个人名片”首屏的基础 UI 骨架代码。

    # Requirements (UI 布局与功能需求 - **原生优先迭代策略**)
    所有视觉元素必须使用 HarmonyOS 原生支持的能力手搓，严禁依赖任何自定义图片或外部资源。
    请在一个 `Column` 容器中自上而下构建以下模块：
    1. **沉浸式头部（Profile Section）**：
    - 包含一个居中的圆形占位组件，使用 `Circle` 配合一个实心背景色（如鸿蒙蓝），并在其内部通过 `Symbol`（符号图标组件）展示一个默认的人形图标。
    - 在其下方，通过 `Text` 组件展示我的姓名和求职意向。
    2. **能力标签区（Skill Tags）**：
    - 使用 `Flex` 或 `Grid` 布局。
    - 生成 3-4 个手搓的标签，每个标签由一个带有边框和圆角的 `Row` 组件、一个相关的原生小图标（Symbol）和一段文字组成。
    3. **项目经历瀑布流（Projects Waterfall）**：
    - 使用 `WaterFlow` 组件构建一个作品集展示区的骨架。
    - 提供 2-3 个 mock 数据源。
    - 水瀑流里的每一个卡片是一个简单的纯色矩形块，仅显示一个标题和一段简短描述。在卡片上预留路由跳转逻辑的占位注释。
    4. **底层联系悬浮按钮**：
    - 在页面右下角或底部，放置一个用于弹出联系方式的浮动圆形按钮（如圆形蓝色背景 + 联系人 Symbol 图标）。

    # Constraints (技术约束与规范 - 极度重要)
    - 必须基于最新的 HarmonyOS 官方 API 规范（API 11/12+）。
    - 【强制要求】：绝对禁止使用 V1 版本的状态管理（不要使用 @Component, @State, @Prop, @Link）。
    - 【强制要求】：必须全面使用状态管理 V2 范式！请使用 `@ComponentV2` 装饰自定义组件。内部状态使用 `@Local`，如果涉及父子组件传值请使用 `@Param` 和 `@Event`，监听状态变化使用 `@Monitor`。
    - 【强制要求】：所有图标、图形占位、颜色必须通过 ArkUI 内置组件手搓（颜色使用原生定义，图标使用符号图标 Symbol 库）。
    - 代码包含中文注释即可已有的注释不用动，解释 V2 状态管理和原生 Symbol 图标在此处的性能优势。
     * *需求2.(已完成需求，不满意)
     * # Role
    资深 HarmonyOS 高级技术专家，精通 ArkUI 动效与高级视觉呈现。

    # Context
    我正在迭代我的“极客个人名片”元服务首页（Index.ets）。目前页面骨架已初步成型，但视觉和交互略显扁平。我需要全面提升 UI 的高级感。

    # Task
    请基于目前的 V2 状态管理骨架，完成以下两项视觉与交互升级：

    毛玻璃与高阶渐变背景：将页面的纯色背景替换为带有微弱动态感的线性渐变色（如深邃蓝到暗夜黑的过渡）。在“能力标签区”和“项目卡片”上，使用 HarmonyOS 原生的 backgroundBlurStyle（如 BlurStyle.Thin）替代纯色背景，营造通透的亚克力悬浮感。

    原生半模态交互 (BindSheet)：重写右下角的联系悬浮按钮（FAB）逻辑。当点击按钮时，使用 bindSheet 弹出一个半模态面板（高度约为屏幕的 1/3）。

    面板内部需包含：我的微信二维码占位（使用原生的 QRCode 组件生成）、一段优雅的打招呼文字，以及一键复制邮箱的按钮。

    必须带有平滑的弹出和回弹动效。

    # Constraints

    继续严格遵守 ArkUI V2 状态管理范式（@Local, @ObservedV2 等）。

    所有的样式调整必须利用原生属性，不依赖任何第三方切图。
    * 需求3. (迭代前面两个需求，并进行优化)
  # Role (角色设定)
你现在是一位拥有深厚底层渲染功底的资深 HarmonyOS 架构师与顶级极客视觉动效专家。你需要协助我完成代码编写，展现出对 ArkUI 引擎、系统底层服务（音频/传感器）以及 API 12 新特性的极致掌控力。

# Context (背景上下文)
我正在开发一款基于 API 12 的 HarmonyOS 元服务（Atomic Service）“极客个人名片”。目前的基础 UI 骨架和 ArkUI V2 状态管理已经搭建完毕。
为了在高级开发岗位的面试中形成“降维打击”，我需要彻底打破单页面的扁平感，对名片进行“满血重构”。引入视、听、触及物理空间联动的全感官交互，打造一个令人惊艳的原生极客作品。

# Task (核心任务)
请基于目前的 V2 状态管理骨架，为我规划并分步实现包含以下四个维度的沉浸式极客名片架构重构：

# Requirements (核心架构与动效需求)
请将代码拆分为合理的目录结构（Managers, Components, Pages, Utils），并实现以下技术点：

1. **听觉与触觉全感官联动 (Audio & Haptics)**：
   - 引入 `@kit.MediaKit`，封装全局后台 `AVPlayer` 单例，用于播放极客风格的背景白噪音或自定义 BGM，并带有入场淡入效果。
   - 结合 `@kit.SensorServiceKit` (或 vibrator)，为所有的可交互元素（Skill Tags、项目卡片、FAB 悬浮按钮）添加细腻的线性马达微震动反馈（类似机械按键的段落感）。

2. **API 12 核心路由与英雄动画 (Navigation & GeometryTransition)**：
   - 彻底废弃老旧的 `router` 机制，根节点全面拥抱 `Navigation` 和 `NavDestination` 架构。
   - 为“项目经历瀑布流”的卡片实现 `GeometryTransition`（共享元素几何转场）。点击卡片时，元素必须以平滑的 Spring 弹性曲线无缝扩展为全屏的项目详情页，呈现出极致的丝滑动效。

3. **重力感应视差交互 (Sensor 裸眼 3D)**：
   - 接入底层传感器 `@kit.SensorKit`（加速度计/陀螺仪）。
   - 监听设备的空间倾斜数据，配合 UI 层级（Z轴），让页面的前景（头像区、名片卡）与动态背景产生微弱的相对位移偏差，营造通透的物理悬浮视差感。

4. **高性能粒子流动效背景 (Canvas 离屏渲染)**：
   - 移除简单的线性渐变背景。在底层铺设 `Canvas` 组件。
   - 纯手搓一套基于时间的帧动画（如：科技感连线粒子流、或缓缓飘落的代码字符阵列）。
   - 结合 `@Local` 状态驱动，确保渲染逻辑脱离 UI 主线程阻塞，挑战 120Hz 满帧运行。

# Constraints (技术约束与极客规范)
- 【强制要求】：继续严格遵循 ArkUI V2 状态管理范式（`@ObservedV2`, `@Trace`, `@ComponentV2`, `@Local`, 跨组件使用 `@Param` / `@Event`）。
- 【强制要求】：所有底层 API 的调用必须符合 HarmonyOS 最新官方文档规范（特别是 API 12 的废弃和推荐用法）。
- 【代码规范】：展现企业级工程素养。必须使用标准的 TypeScript 面向对象思维封装 Manager 类。关键动画和底层调用逻辑需配有专业的中英文双语注释。
     */
  }
}
